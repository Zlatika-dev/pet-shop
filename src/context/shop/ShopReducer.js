import {
  ADD_ITEM,
  SHOW_FULLIMG,
  DELETE_ITEM,
  SHOW_NAV,
  CLOSE_NAV,
  SHOW_MOBILE_NAV,
  HIDE_MOBILE_NAV
} from '../types';

export default (state, action) => {
  switch (action.type) {
   
    default:
      return state
  }
}